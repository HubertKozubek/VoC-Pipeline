---
trigger: always_on
---

To run anything use **uv**

This project uses uv to help with package menagment, so:
* if you want to install any package use uv add for that `uv add <package>`
* if you want to run a script, use uv run for that `uv run script.py`
