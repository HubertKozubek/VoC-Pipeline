---
trigger: always_on
---

If you want to import something, always import at the top of the file, don't add imports in the middle of the file
All the neccessary import should be at the top fo the file